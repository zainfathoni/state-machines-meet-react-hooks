import { CodeSurferLayout } from 'code-surfer'

# State Machine Introduction

---

<CodeSurferLayout>

```js 1:3,5 title="A simple toggle machine example"
import { Machine } from 'xstate'

const toggleMachine = Machine({
  //
})
```

```js 4:4 subtitle="Initial state"
import { Machine } from 'xstate'

const toggleMachine = Machine({
  initial: 'inactive',
})
```

```js 5:6,8:9,11:12 subtitle="States"
import { Machine } from 'xstate'

const toggleMachine = Machine({
  initial: 'inactive',
  states: {
    inactive: {
      //
    },
    active: {
      //
    },
  },
})
```

```js 7:9,12:14 subtitle="State Transitions"
import { Machine } from 'xstate'

const toggleMachine = Machine({
  initial: 'inactive',
  states: {
    inactive: {
      on: {
        TOGGLE: 'active',
      },
    },
    active: {
      on: {
        TOGGLE: 'inactive',
      },
    },
  },
})
```

```js 3:17 subtitle="Copy these lines"
import { Machine } from 'xstate'

const toggleMachine = Machine({
  initial: 'inactive',
  states: {
    inactive: {
      on: {
        TOGGLE: 'active',
      },
    },
    active: {
      on: {
        TOGGLE: 'inactive',
      },
    },
  },
})
```

</CodeSurferLayout>

---

# Visualize

Paste to the <a target="\_blank" href="https://statecharts.github.io/xstate-viz/">XState visualizer</a>
